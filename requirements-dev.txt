# Dev-only tooling (linting / formatting / type-checking).
# Production dependencies live in requirements.txt — do NOT merge these in.
# Install:  pip install -r requirements-dev.txt
ruff==0.15.15
mypy==2.1.0
