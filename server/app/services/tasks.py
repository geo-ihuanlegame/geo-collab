"""Compatibility shim — re-exports from modules/tasks/."""
from server.app.modules.tasks.task_Crud import (  # noqa: F401
    ACTIVE_RECORD_STATUSES,
    CAN_RETRY_TASK_STATUSES,
    PAUSED_RECORD_STATUSES,
    TERMINAL_TASK_STATUSES,
    VALID_TASK_TYPES,
    AssignmentItem,
    TaskInputs,
    add_log,
    aggregate_task_status,
    create_task,
    delete_all_tasks,
    get_record,
    get_task,
    list_task_logs,
    list_task_records,
    list_tasks,
    manual_confirm_record,
    preview_task_assignment,
    recover_stuck_records,
    recover_stuck_task_claims,
    resolve_user_input_record,
    retry_record,
    # private helpers needed by tests
    _validated_task_inputs,
    _build_assignments,
    _validated_accounts,
    _validate_unique_articles,
    _article_ids_for_task,
)
from server.app.modules.tasks.task_Executor import (  # noqa: F401
    MAX_CONCURRENT_RECORDS,
    WORKER_LEASE_EXTENSION_SECONDS,
    RunningRecord,
    RecordPublishOutcome,
    _task_locks,
    _account_locks,
    _account_locks_lock,
    _global_publish_sem,
    _task_cancel,
    build_publish_runner_for_record,
    cancel_task,
    execute_task,
    # private helpers needed by tests
    _heartbeat_task_worker,
    _task_cancel_requested,
    _request_task_cancel,
    _cancel_not_running_records,
    _run_pending_records,
    _start_runnable_records,
    _try_acquire_account_lock,
    _release_account_lock,
    _claim_record,
    _load_article_for_publish,
    _validate_record_inputs,
    _detach_record_inputs,
    _publish_record,
    _finish_record_future,
    _diagnostics_from_exception,
    _add_publish_diagnostics,
    _mark_record_failed,
    _mark_record_waiting_user_input,
    _store_failure_screenshot,
)
