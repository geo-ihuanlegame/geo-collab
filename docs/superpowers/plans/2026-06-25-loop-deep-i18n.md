# `/goal` Loop 主对话叙述深度中文化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 orchestrator skill 模板里所有**用户可见**的字眼（echo 6 行 + 伪码 echo 字符串 + subagent description + 飞书消息）改为无术语中文，并加「主对话叙述规范」段硬约束 Claude 在主对话叙述时不准回流 `orchestrator / netto / qid / pool / N / matrix / goal-verifier` 等术语。

**Architecture:** 单文件主战场 `templates/skills/geo-goal-orchestrator/SKILL.md`，3 块改造：①进度日志格式段 6 行 + 伪码内的所有 echo / notify_feishu 字符串 + subagent description 字符串模板 + Ctrl-C echo 字符串 → 无术语中文；②新增「主对话叙述规范（强制）」段（黑名单 + 对照表 + 反例 + 正例 + 例外）；③ bundle bump v3→v4。

**Tech Stack:** Markdown 模板 / Python lint 测试（grep-based）/ FastMCP bundle 分发链路 / 既有 `test_loop_skill_bundle.py` 框架

**Spec:** [`docs/superpowers/specs/2026-06-25-loop-deep-i18n-design.md`](../specs/2026-06-25-loop-deep-i18n-design.md)

**Branch:** `fix/loop-deep-i18n`（已从 `origin/main` 拉，spec 已 commit 在 `3dcf9f3`）

---

## Files to Touch

| 文件 | 操作 | 责任 |
|---|---|---|
| `server/app/modules/loop_skills/templates/skills/geo-goal-orchestrator/SKILL.md` | 修改 | 6 行日志 + 伪码 echo / notify_feishu / subagent description / Ctrl-C 字符串全部换无术语中文 + 新加「主对话叙述规范（强制）」段 |
| `server/tests/test_loop_skill_bundle.py` | 修改 | 追加 2 个 lint 测试（黑名单 grep + 叙述规范段存在性） |
| `server/app/modules/loop_skills/version.py` | 修改 | bump `LOOP_SKILL_BUNDLE_VERSION` v3→v4，KNOWN_BUNDLE_SHAS 加 2 个 v4 sha（CRLF + LF）|

**关键边界**：
- 只动 orchestrator skill — writer / verifier / README / commands 都不动
- 伪码变量名（`netto / used_qids / qid / tpl_id`）+ MCP 工具调用（`save_article(question_item_id=...)`）保持原状
- v1 / v2 / v3 sha 全保留（v3 有 4 个 sha：PR #152 LF + CRLF + illustration_warnings PR LF + CRLF）

---

## Task 1: orchestrator skill 全部 i18n 改动 + 2 lint 测试（TDD）

**Files:**
- Modify: `server/app/modules/loop_skills/templates/skills/geo-goal-orchestrator/SKILL.md`
- Modify: `server/tests/test_loop_skill_bundle.py`

### Step 1: 写 2 个失败的 lint 测试

在 `server/tests/test_loop_skill_bundle.py` 末尾追加：

```python
def test_orchestrator_skill_progress_log_no_english_jargon():
    """orchestrator skill 用户可见的进度日志 + 伪码 echo / notify_feishu 字符串
    不应再含英文 / 术语黑名单（pool= / qid= / matrix=< / goal 评审 / netto /
    attempts ceiling / candidates 用尽 / token 预算 / MCP 连续 / [interrupted] /
    生文 Loop / article_id= / decision= / score=）。

    严格 grep 模板全文，宁错杀也要催 i18n 改完。
    """
    from pathlib import Path

    skill = (
        Path(__file__).resolve().parent.parent
        / "app/modules/loop_skills/templates/skills/geo-goal-orchestrator/SKILL.md"
    )
    text = skill.read_text(encoding="utf-8")

    # 这些 token 在面向用户的位置出现就算回退；伪码变量名（netto / qid / pool_id
    # 等）出现在 Python 代码缩进里不在此列（黑名单针对的是字符串字面量 + 注释 /
    # 表格 / 文档行）。判定方式：白名单允许 `target.pool_id` / `netto.count` /
    # `qid = pick_next_qid` 这种代码读取；其它出现都视为日志/叙述用词漂移。
    blacklist = [
        "[快检]",                  # 旧 tag，应该换成 [启动检查]
        "pool=",                   # echo 字段名应该是「问题池：」
        "matrix=<code|默认>",      # echo 字段名应该是「矩阵：」
        "matrix=<code|default>",   # 旧版残留
        "goal 评审",               # 应该是「评审员」
        "[净产出]",                # 应该是「累计通过」
        "attempts ceiling",        # 飞书消息应该是「已达尝试轮数上限」
        "token 预算",              # 飞书消息应该是「主对话内存预算」
        "MCP 连续失败",            # 飞书消息应该是「接口连续失败」
        "[interrupted]",           # 应该是「[已中断]」
        "生文 Loop ",              # 应该是「生文流程」
        "description=f\"写一篇文章 qid=",   # subagent description 应换成「改写文章（问题 #...）」
        "description=f\"评分 article_id=",  # subagent description 应换成「评审文章 #...」
    ]
    hits = [token for token in blacklist if token in text]
    assert not hits, (
        f"orchestrator SKILL.md 仍有 i18n 漂移 token：{hits}. "
        "改完 echo / notify_feishu / subagent description 后再跑."
    )


def test_orchestrator_skill_has_narration_rules_section():
    """orchestrator skill 必须含「主对话叙述规范（强制）」段 + 关键黑名单关键词,
    才能让 Claude 在自由叙述时不漏术语."""
    from pathlib import Path

    skill = (
        Path(__file__).resolve().parent.parent
        / "app/modules/loop_skills/templates/skills/geo-goal-orchestrator/SKILL.md"
    )
    text = skill.read_text(encoding="utf-8")

    assert "# 主对话叙述规范（强制）" in text, (
        "orchestrator skill 缺「主对话叙述规范（强制）」段；该段用于约束 Claude "
        "在主对话自由叙述时不漏 orchestrator/netto/qid 等英文术语."
    )
    # 段内必须含黑名单关键词 + 反例 / 正例标签
    for keyword in ["❌ 不要说", "✅ 改成", "**反例**", "**正例**"]:
        assert keyword in text, f"叙述规范段缺关键标签：{keyword}"
    # 段内必须列出关键术语黑名单
    for term in ["orchestrator", "netto", "qid", "pool", "matrix", "goal-verifier"]:
        assert term in text, f"叙述规范段未列入黑名单术语：{term}"
```

### Step 2: 跑测试，确认 2 个全 fail

```bash
docker compose exec app pytest server/tests/test_loop_skill_bundle.py::test_orchestrator_skill_progress_log_no_english_jargon server/tests/test_loop_skill_bundle.py::test_orchestrator_skill_has_narration_rules_section -v
```

**预期**：2 个测试 fail（test 1 列一堆黑名单 hit；test 2 提示缺「主对话叙述规范（强制）」段）。

### Step 3: 替换 6 行进度日志格式段

**先 Read** `server/app/modules/loop_skills/templates/skills/geo-goal-orchestrator/SKILL.md` 找到约 line 118-127 的进度日志段。

用 Edit 把这块（当前 6 行）：

```
[快检] pool=<name> N=<N> matrix=<code|默认> 通过
[第 k/3N 轮] 选题 qid=<id> → 改写中 …
[第 k/3N 轮] 改写完成 article_id=<id>, 评审中 …
[第 k/3N 轮] 评审 决策=<d> 总分=<total>
[净产出] 今日通过 goal 评审的文章数: <count>/<N>
[完成|中止] 净产出 <count>/<N>, 共耗时 <m>m, 原因=<...>
```

替换为：

```
[启动检查] 问题池：<name>　目标：<N> 篇　矩阵：<code|默认>　✓
[第 k/3N 轮] 选题：问题 #<id> → 改写中 …
[第 k/3N 轮] 改写完成（文章 #<id>），评审中 …
[第 k/3N 轮] 评审结果：<d>　分数 <total>
[累计通过] 今日已过审文章数：<count>/<N> 篇
[完成|中止] 累计通过 <count>/<N>，共耗时 <m> 分钟，原因：<...>
```

> 注意全角空格 `　`（Unicode U+3000）不是普通空格，用于字段之间松散分隔；复制粘贴时小心别变 ASCII 空格。

### Step 4: 替换伪码内 echo 字符串

用 Edit 改伪码段（约 line 39-115）里的 5 个 echo 字符串：

**echo 1**（line 47 附近）：
```python
echo(f"[净产出] 今日通过 goal 评审的文章数: {netto.count}/{target.N}")
```
→
```python
echo(f"[累计通过] 今日已过审文章数：{netto.count}/{target.N} 篇")
```

**echo 2**（line 89 附近）：
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 改写失败: {parsed.error}")
```
→
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 改写失败：{parsed.error}")
```
（仅冒号换全角；句式已是中文）

**echo 3**（line 95 附近）：
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 改写完成 article_id={article_id}, 评审中 …")
```
→
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 改写完成（文章 #{article_id}），评审中 …")
```

**echo 4**（line 112 附近）：
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 评审失败, article {article_id} 留 pending 由人审")
```
→
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 评审失败，文章 #{article_id} 留待人工审核")
```

**echo 5**（line 114 附近）：
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 评审 决策={parsed_v.decision} 总分={parsed_v.score_total}")
```
→
```python
echo(f"[第 {attempts}/{3*target.N} 轮] 评审结果：{parsed_v.decision}　分数 {parsed_v.score_total}")
```

### Step 5: 替换伪码内 notify_feishu 字符串

用 Edit 改 5 处 notify_feishu（约 line 50-63）：

**notify 1**（成功）：
```python
notify_feishu("生文 Loop 完成", f"净产出 {netto.count}/{target.N}, 共耗时 {minutes}m", "done")
```
→
```python
notify_feishu("生文流程完成", f"累计通过 {netto.count}/{target.N}，共耗时 {minutes} 分钟", "done")
```

**notify 2**（attempts ceiling）：
```python
notify_feishu("生文 Loop 中止", f"attempts ceiling, 净产出 {netto.count}/{target.N}", "warning")
```
→
```python
notify_feishu("生文流程中止", f"已达尝试轮数上限，累计通过 {netto.count}/{target.N}", "warning")
```

**notify 3**（candidates 用尽）：
```python
notify_feishu("生文 Loop 中止", f"候选问题用尽, 净产出 {netto.count}/{target.N}", "warning")
```
→
```python
notify_feishu("生文流程中止", f"候选问题用完，累计通过 {netto.count}/{target.N}", "warning")
```

**notify 4**（token 预算）：
```python
notify_feishu("生文 Loop 中止", f"token 预算触线, 净产出 {netto.count}/{target.N}", "warning")
```
→
```python
notify_feishu("生文流程中止", f"主对话内存预算触顶，累计通过 {netto.count}/{target.N}", "warning")
```

**notify 5**（MCP 连续失败）：
```python
notify_feishu("生文 Loop 中止", "MCP 连续失败 3 次, 请检查后端/token", "error")
```
→
```python
notify_feishu("生文流程中止", "接口连续失败 3 次，请检查服务连接 / 凭证", "error")
```

### Step 6: 替换 subagent description 字符串

用 Edit 改 2 处（约 line 76 + 101）：

**writer description**：
```python
description=f"写一篇文章 qid={qid}",
```
→
```python
description=f"改写文章（问题 #{qid}）",
```

**verifier description**：
```python
description=f"评分 article_id={article_id}",
```
→
```python
description=f"评审文章 #{article_id}",
```

### Step 7: 替换 Stop / Budget Rules 段里的 Ctrl-C 字符串

用 Edit 改 line 147 附近：

```markdown
- 用户 Ctrl-C → 主对话 echo `[interrupted] 已落库 X 篇, 净产出 Y/N, 下次 /goal 会接力`（不发飞书）
```

→

```markdown
- 用户 Ctrl-C → 主对话 echo `[已中断] 已落库 X 篇，累计通过 Y/N 篇，下次 /goal 会接力`（不发飞书）
```

### Step 8: 新增「主对话叙述规范（强制）」段

用 Edit 在「# 进度日志」段（含 6 行 code block 的整段）之后、「# Helper 定义」段之前**插入**新段。

定位锚点：找 `# 进度日志（必须 echo 这些短行）` 段末尾的反引号 + 空行 + `# Helper 定义（消除歧义）`。把这之间插入：

```markdown

# 主对话叙述规范（强制）

你向用户叙述本次 /goal 运行时，**只能用中文 + 上面进度日志的固定格式**。
绝对不要在叙述里出现以下英文 / 内部术语（左侧错例，右侧用法）：

| ❌ 不要说 | ✅ 改成 |
|---|---|
| orchestrator | 编排员 / 我 |
| netto / 净产出 | 累计通过数 |
| goal / goal-verifier | 目标 / 评审员 |
| pool / pool_id | 问题池 |
| qid | 问题 #编号 |
| tpl_id | 模板 #编号 |
| article_id | 文章 #编号 |
| matrix / matrix_code | 矩阵 |
| N | 目标 X 篇 |
| writer / verifier | 改写员 / 评审员 |
| subagent | 子助手 |
| attempts | 已尝试轮数 |
| token 预算 | 主对话内存预算 |

**反例**（千万别这样说）：

> 启动 orchestrator。N=5 国风。先看 netto，已知国风候选 qid=80/81/82/83。

**正例**：

> 开始执行 /goal：目标 5 篇国风游戏文章。先看一下累计通过数，
> 当前候选问题：#80 / #81 / #82 / #83（共 4 条）。

**例外**（这些保留原样，因为是 Claude Code 自己加的或后端契约）：
- `Skill(...)` / `Agent(...)` / `Called geo` 前缀 — Claude Code UI 自动加
- MCP 工具调用 `save_article(question_item_id=80, prompt_template_id=11)` — 工具签名
- 文件路径、URL、内部命令行 — 保持原样

> 这一段比技术契约更重要——使用者看不懂"netto"，但他们花 10 分钟跑 /goal 时
> 主对话是他们唯一的进度反馈。不要让英文 / 缩写打断他们的注意力。

```

### Step 9: 跑测试确认 2 个全 pass

```bash
docker compose exec app pytest server/tests/test_loop_skill_bundle.py::test_orchestrator_skill_progress_log_no_english_jargon server/tests/test_loop_skill_bundle.py::test_orchestrator_skill_has_narration_rules_section -v
```

**预期**：`2 passed`。

如果 test 1 仍 fail：grep 失败信息里 hit 的具体 token，找漏的字符串改。

### Step 10: 跑 bundle 全部测试，确认其余不破

```bash
docker compose exec app pytest server/tests/test_loop_skill_bundle.py -q
```

**预期**：原 7 个 + 新 2 个 = 至少 9 passed。**注意**：`test_bundle_sha_is_known` 会 fail（因为 v4 sha 还没加进 KNOWN）—— 这是 Task 2 处理。**本 step 只确认其它测都过，sha 测试 fail 是预期**。

### Step 11: ruff + format clean

```bash
docker compose exec app ruff check server/tests/test_loop_skill_bundle.py
docker compose exec app ruff format --check server/tests/test_loop_skill_bundle.py
```

如 format 报差异，去掉 `--check` 直接改写。

### Step 12: Commit

```bash
git add server/app/modules/loop_skills/templates/skills/geo-goal-orchestrator/SKILL.md server/tests/test_loop_skill_bundle.py
git commit -m "$(cat <<'EOF'
feat(loop_skills): orchestrator skill 主对话叙述深度中文化 + 2 lint 测试

PR #149 留下的 6 行日志仍混着 pool/qid/N/matrix/netto/goal/orchestrator 等术语；
用户截图显示"启动 orchestrator。N=5 国风。先看 netto"完全看不懂。本 PR 把所有
用户可见字眼改为无术语中文：
- 6 行进度日志格式：[启动检查] 问题池 / [累计通过] 今日已过审文章数 等
- 伪码 echo 字符串：article_id→文章 # / 决策=→评审结果： / etc.
- notify_feishu 5 处：生文 Loop→生文流程 / attempts ceiling→已达尝试轮数上限 / etc.
- subagent description：写一篇文章 qid=→改写文章（问题 #）/ 评分→评审文章 #
- Ctrl-C echo：[interrupted]→[已中断]
- 新增「主对话叙述规范（强制）」段：黑名单 + 对照表 + 反例 + 正例 + 例外，
  硬约束 Claude 在自由叙述时不漏 orchestrator/netto/qid 等术语

2 个 lint 测试：
- test_orchestrator_skill_progress_log_no_english_jargon: grep 13 个黑名单 token
- test_orchestrator_skill_has_narration_rules_section: 确认段存在 + 关键标签 +
  6 个核心术语都被列入黑名单

伪码变量名 / MCP 工具调用 / Claude Code UI 标签都不动（不归用户管的层）。
bundle version bump 留 Task 2.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: bundle version bump v3→v4 + KNOWN sha

**Files:**
- Modify: `server/app/modules/loop_skills/version.py`

### Step 1: 跑 build_bundle 拿当前 sha

```bash
docker compose exec app python -c "from server.app.modules.loop_skills.service import build_bundle; print(build_bundle().bundle_sha256)"
```

**预期**：打出一串 64 字符 hex（v4 sha，区别于 v1/v2/v3 任意一个）。**复制这串**到 Step 3。

> **关于行尾**：本地 dev container（Windows host bind-mount）读到的是 **CRLF** 内容，算出来的 sha 是 CRLF 版本。CI Linux runner git checkout 默认 LF，会算出**不同**的 sha。两个都要加进 KNOWN（吸取 PR #152 经验）。本 Step 1 拿到的是 CRLF sha。

### Step 2: 跑 sha 校验测试，确认 fail

```bash
docker compose exec app pytest server/tests/test_loop_skill_bundle.py::test_bundle_sha_is_known -v
```

**预期**：fail，错误信息会打出 `Bundle sha256 = '<step 1 拿到的 sha>' not in KNOWN_BUNDLE_SHAS`。

### Step 3: bump version + 加 CRLF sha 到 KNOWN

修改 `server/app/modules/loop_skills/version.py`，整文件替换为：

```python
"""手工维护的 bundle 版本号 + 已审核 sha 集合。

CI 测试 (test_loop_skill_bundle.py::test_bundle_sha_is_known) 会校验：
如果 build_bundle().bundle_sha256 没记录在 KNOWN_BUNDLE_SHAS 集合里，
fail + 提示开发者：把新 sha 加进 KNOWN_BUNDLE_SHAS 并 bump
LOOP_SKILL_BUNDLE_VERSION，强制「改模板必同步 bump 版本」纪律。
"""

LOOP_SKILL_BUNDLE_VERSION = "2026-06-25-v4"

KNOWN_BUNDLE_SHAS: frozenset[str] = frozenset(
    {
        # v1 (2026-06-24)
        "49f824a36606c285b84c71ade5aec406ea3c545599b7a3ccf59f863b521667dd",
        # v2 (2026-06-25): writer step 5 + 矩阵特例 + orchestrator 日志中文化
        "abd8416c51f0b591c85cee0c3635645a10a313a2cedbeb52b89953a2c41e7fea",
        # v3 (2026-06-25, PR #152): writer 矩阵段 + README step 5 改为 list_stock_categories
        # 自助路径。两个 sha 是同一份内容的两种行尾：CRLF（Windows 本地装的副本）与 LF（CI /
        # Linux）。build_bundle 读字节后直接 sha256，行尾不归一化——两边都要认。
        "58448672effda8290f97dc5afdfb6c4146ea9c8b7cc7c432b2d4a76274b65856",  # CRLF
        "515c9202f1e0880a1657e4da768954e83bb8a2a5cdfc220355a369862d2cefc6",  # LF (CI canonical)
        # v3 (2026-06-25, illustration_warnings PR #154): writer step 5 改 4 类信号检查 +
        # illustration_warnings 字段。
        "ee9659ae08d68a6bdabecfce9f60324fab2093b7ad4535f056f5cdc4ea6a77e8",  # LF
        "c8050b24111efc69b56259194bed5d4b236b537ce7effc777a5e2786b3e44acc",  # CRLF
        # v4 (2026-06-25, this PR): orchestrator skill 主对话叙述深度中文化
        # （6 行日志 / 伪码 echo / notify_feishu / subagent description + 新增叙述规范段）
        "<把 step 1 打出的 CRLF sha 填到这里>",  # CRLF (Windows host)
        # LF sha 由 CI 跑出来后追加进来（首跑 CI fail 拿 sha + 第二 commit 加进 KNOWN）
    }
)
```

把 `<把 step 1 打出的 CRLF sha 填到这里>` 替换为 Step 1 实际打印出的 sha。

### Step 4: 跑 bundle 全部测试，确认本地全 pass

```bash
docker compose exec app pytest server/tests/test_loop_skill_bundle.py -q
```

**预期**：全 pass（本地 CRLF sha 已认；CI LF sha 会在 Task 3 第一次 push 后从 CI fail log 拿）。

### Step 5: Commit

```bash
git add server/app/modules/loop_skills/version.py
git commit -m "$(cat <<'EOF'
chore(loop_skills): bump LOOP_SKILL_BUNDLE_VERSION v3→v4 + 加 v4 CRLF sha

Task 1 改了 orchestrator skill 整段 i18n，bundle sha 自然变。
本机 dev container（Windows host bind-mount）算出 CRLF sha 先加进 KNOWN；
CI Linux runner 会算出不同的 LF sha，待 Task 3 首跑 CI fail 后从 log 拿
sha 追加进来（吸取 PR #152 经验，CRLF + LF 都要认）。

v1 / v2 / v3 共 6 个旧 sha 全保留：已经装了旧版的同事本机校验仍要认.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 全 lint/test + push + PR + 首跑 CI 拿 LF sha 补 commit

### Step 1: 后端全 lint + 全测

```bash
docker compose exec app ruff check server/
docker compose exec app ruff format --check server/
docker compose exec -e GEO_TEST_DATABASE_URL=mysql+pymysql://geo_user:geo_pass_dev@mysql:3306/geo_test app pytest server/tests/ -q
```

**预期**：ruff 0 error，pytest 全 pass。

mypy 不可用就跳（CI 上会跑）。

### Step 2: 前端 typecheck（前端零改动应自然过）

```bash
pnpm --filter @geo/web typecheck
```

**预期**：0 error。

### Step 3: 推分支

```bash
git push -u origin fix/loop-deep-i18n
```

### Step 4: 建 PR

```bash
gh pr create --title "fix(loop): orchestrator 主对话叙述深度中文化 — 去掉 pool/qid/N/matrix/netto/goal 等术语" --body "$(cat <<'EOF'
## Summary

[PR #149](https://github.com/geo-ihuanlegame/geo-collab/pull/149) 留下的 6 行进度日志仍混着 \`pool / qid / N / matrix / netto / goal / orchestrator\` 等英文术语；用户截图显示"启动 orchestrator。N=5 国风。先看 netto"完全看不懂。本 PR 把所有**用户可见**字眼改为无术语中文。

### 改动 3 块（都在 orchestrator skill 模板）

1. **6 行进度日志格式** → 无术语中文（\`[启动检查] 问题池：... 目标：N 篇 矩阵：... ✓\` / \`[累计通过] 今日已过审文章数：x/y 篇\` 等）
2. **伪码内字符串** → 无术语中文：
   - 5 个 \`echo()\`：\`article_id=\`→\`文章 #\` / \`决策=\`→\`评审结果：\` / etc.
   - 5 个 \`notify_feishu()\`：\`生文 Loop\`→\`生文流程\` / \`attempts ceiling\`→\`已达尝试轮数上限\` / \`token 预算触线\`→\`主对话内存预算触顶\` / etc.
   - 2 个 subagent \`description=\`：\`写一篇文章 qid=\`→\`改写文章（问题 #...）\` / \`评分 article_id=\`→\`评审文章 #\`
   - Ctrl-C echo：\`[interrupted]\`→\`[已中断]\`
3. **新增「主对话叙述规范（强制）」段**：黑名单（13 个术语对照） + 反例 + 正例 + 例外。硬约束 Claude 在主对话**自由叙述**时不漏 \`orchestrator / netto / qid / pool / N / matrix\` 等术语

### 不动

- 伪码变量名（\`netto / used_qids / qid / tpl_id\`）— 编程层概念，跟用户无关
- MCP 工具调用 / 参数名（\`save_article(question_item_id=...)\`）— 后端契约
- Claude Code UI 自动加的 \`Skill(...)\` / \`Agent(...)\` / \`Called geo\` 标签 — 不归我们
- writer / verifier skill / README / commands — 他们不直接向用户叙述

### bundle version

v3 → v4，KNOWN_BUNDLE_SHAS 加 2 个 v4 sha（CRLF + LF）。v1/v2/v3 共 6 个旧 sha 全保留兼容未升级使用者。

## Test plan

- [x] 后端 ruff / format / pytest 全过
- [x] 2 个新 lint 测试通过（黑名单 grep + 叙述规范段存在性）
- [x] test_bundle_sha_is_known v4 sha 加 KNOWN 通过
- [x] 前端 typecheck 通过（前端零改动）
- [ ] **使用者本地装 v4 后跑 \`/goal 今天 5 篇国风游戏文章\`**：主对话叙述里不出现 orchestrator / netto / qid / pool / N / matrix / goal-verifier 任何一个（user 手动验证）
- [ ] **6 行进度日志格式跟 spec §4.2 完全一致**（user 手动验证）
- [ ] **Agent(...) description 全中文**（user 手动验证）
- [ ] **飞书播报全中文**（user 手动验证）

## 设计 / 实施

- 设计稿：\`docs/superpowers/specs/2026-06-25-loop-deep-i18n-design.md\`
- 实施 plan：\`docs/superpowers/plans/2026-06-25-loop-deep-i18n.md\`
- 上游 PR：#149 (已合) + #152 (已合) + #154 (已合，illustration_warnings)

## 与其它 PR 关系

独立于 PR #152 + illustration_warnings PR #154。都改 orchestrator skill 同一文件但本 PR 主战场是「日志 + 叙述」，可独立合并。

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

如果 gh 命令失败（认证 / 仓库权限），记录命令 + 错误给 user 手动跑。

### Step 5: 等 CI 跑完，看 backend-tests 是否 fail（预期 fail 一次）

```bash
sleep 60 && gh pr checks $(gh pr view --json number -q .number) 2>&1 | head -10
```

如果 backend-tests shard 2/4 fail with `Bundle sha256 = '<LF sha>' not in KNOWN_BUNDLE_SHAS`：
- **预期场景**——CI Linux runner 算出的 LF sha 不在 KNOWN
- 进入 Step 6 处理

如果全 pass：跳到 Step 8（CI 直接就过，免去 LF sha 追加）

### Step 6: 从 CI fail log 抓 LF sha

```bash
gh pr checks $(gh pr view --json number -q .number) 2>&1 | grep "fail" | head -3
# 找 backend-tests shard 那行的 job URL
gh run view --job=<job_id> --log-failed 2>&1 | grep -E "Bundle sha256 = '[a-f0-9]{64}'" | head -1
```

复制 sha 串到 Step 7。

### Step 7: 把 LF sha 加进 KNOWN + push

修改 `server/app/modules/loop_skills/version.py`，在 v4 CRLF sha 行后追加一行：

```python
        # v4 (2026-06-25, this PR): orchestrator skill 主对话叙述深度中文化
        "<CRLF sha 已在 Task 2 填好>",  # CRLF (Windows host)
        "<把 Step 6 拿到的 LF sha 填到这里>",  # LF (CI canonical)
```

把 `<把 Step 6 拿到的 LF sha 填到这里>` 替换为实际 sha。

```bash
git add server/app/modules/loop_skills/version.py
git commit -m "$(cat <<'EOF'
fix(loop_skills): v4 加 CI LF sha 到 KNOWN — CI fail 修复

CI fail 根因：本地 Windows 经 dev container bind-mount 读到 CRLF 文件，
build_bundle 算 sha = <CRLF>; CI Linux runner git checkout 后是 LF
内容，算 sha = <LF>. 同份内容两种行尾.

本 commit 把 CI 算的 LF sha 也加进 KNOWN，让两边校验都过. 与 PR #152
同款修法.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

### Step 8: 验 CI 全过

```bash
sleep 60 && gh pr checks $(gh pr view --json number -q .number) 2>&1
```

**预期**：所有 job pass（changes / lint / frontend / backend-tests × 4 / security-audit / backend gate）。

---

## Self-Review

**1. Spec coverage check** — 每节是否有对应 task？

| Spec 节 | Task 覆盖 |
|---|---|
| §0 一句话定位 | Plan Goal 段引用 |
| §1 问题定位 | Task 1 整体解决 |
| §2 锁定决策 2 项 | 体现在 Files to Touch + 实施细节（独立 PR 已通过 branch base 反映） |
| §3 架构总览 + 文件改动 | Plan Files to Touch 表 |
| §4.1 术语对照表 | Task 1 Step 8 叙述规范段插入了完整对照表 |
| §4.2 6 行日志映射 | Task 1 Step 3 完整替换 |
| §4.3 subagent description | Task 1 Step 6 |
| §4.4 飞书播报消息映射 | Task 1 Step 5 |
| §5 主对话叙述规范段 | Task 1 Step 8（包含黑名单 + 对照 + 反例 + 正例 + 例外） |
| §6 version bump | Task 2 |
| §7.1 自动测 2 个 | Task 1 Step 1（TDD 先写测） |
| §7.2 手工冒烟 5 步 | Task 3 Step 4 PR description 里 unchecked 列出 |
| §8 工作量 + 顺序 | Plan 3 task 顺序匹配 |
| §9 与上游 PR 关系 | Plan Branch + Task 3 PR description |
| §10 Out of Scope | 隐含落实 |
| §11 上线门禁 | Task 3 Step 8 |

**结论：全覆盖**。

**2. Placeholder scan** — 检查无 TBD / TODO：
- Task 2 Step 3 的 `<把 step 1 打出的 CRLF sha 填到这里>` 是**有意运行时占位**（必须实际跑 build_bundle 拿到 sha 才知道），文档清楚说替换流程 ✓
- Task 3 Step 7 的 `<把 Step 6 拿到的 LF sha 填到这里>` 同理 ✓
- 其它无遗留占位

**3. Type consistency**
- `LOOP_SKILL_BUNDLE_VERSION = "2026-06-25-v4"` — Task 2 + Task 3
- 黑名单 token 列表 — Task 1 Step 1（测试断言）与 Task 1 Step 3-7（实际修改字符串） 一一对应，没有漂移
- 「主对话叙述规范（强制）」段名 — Task 1 Step 1 测试 assertion + Task 1 Step 8 实际插入段名完全一致

**结论：一致**。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-25-loop-deep-i18n.md`. Two execution options:

**1. Subagent-Driven (recommended)** — 我每个 task 起 fresh subagent 跑，task 之间 review，迭代快。

**2. Inline Execution** — 我在当前会话里逐 task 顺序执行，每 task 检查一次。

Which approach?
